/* Material.js */

class Material {
    constructor (color, refraction) {
        this.color = color || new Vector();
        this.refraction = refraction || 1;
    }

// may need methods eventually
}
