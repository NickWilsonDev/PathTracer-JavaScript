/* Sphere.js */

class Sphere {
    constructor (center, radius, material) {
        this.center = center;
        this.radius = radius;
        this.material = material;
        this.radiusSquared = this.radius * this.radius;
    }

// need intersection methods
    intersection(ray) {
    // calculate values used for finding determinant

    // only hits if determinant in 0 or greater


    //return distance to hitpoint?
    }
}
